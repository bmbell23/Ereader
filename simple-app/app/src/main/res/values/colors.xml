<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Backdrop behind the adaptive foreground, visible in the bleed area on round /
         squircle masks. The new foreground is a full-bleed gradient (opaque to the
         corners), so this rarely shows — set to an on-brand mid-purple (not white) so
         any sliver at the mask edge blends with the icon instead of flashing white. #133 -->
    <color name="ic_launcher_background">#7F30AF</color>
</resources>
